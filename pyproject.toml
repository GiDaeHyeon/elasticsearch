[tool.poetry]
name = "elasticsearch"
version = "0.1.0"
description = "ElasticSearch Practice Enviroment"
authors = ["GiDaeHyeon"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
